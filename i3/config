# Set resolution of screen
# very usefull on VMs
exec --no-startup-id xrandr --output Virtual1 --mode 1680x1050
exec --no-startup-id nitrogen --restore

# Custom key bindings
bindsym $mod+shift+F12 exec i3lock --image=/home/adrien.josse/afs/Pics/PROJEEET.PNG
